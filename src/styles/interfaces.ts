export interface IBody{
    color?: String;
}

export interface IElement{
    right?: Boolean;
    left?: Boolean;
    top?: Boolean;
    bottom?: Boolean;
}

export interface ILayer{
    color: String;
    position: Number;
}

export interface ITitle{
    margin: String;
}